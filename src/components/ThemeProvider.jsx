import React from 'react';

/**
 * Stub minimal pour ThemeProvider
 * Ce composant est un placeholder pour permettre la compilation
 */
const ThemeProvider = ({ children }) => {
  return <>{children}</>;
};

export default ThemeProvider; 