import React from 'react';

/**
 * Stub minimal pour AppInitializer
 * Ce composant est un placeholder pour permettre la compilation
 */
const AppInitializer = ({ children }) => {
  return <>{children}</>;
};

export default AppInitializer; 