import React, { useState } from 'react'; import TimerSettings from './TimerSettings'; const AIInteractionConfig = ({ sessionConfig = {}, updateSessionConfig, mode = 'standard' }) => { const handleTimerEnabledChange = (enabled) => { updateSessionConfig({ ...sessionConfig, timerEnabled: enabled, settings: { ...sessionConfig.settings, ai_configuration: { ...(sessionConfig.settings?.ai_configuration || {}), timerEnabled: enabled } } }); }; const handleTimerDurationChange = (duration) => { updateSessionConfig({ ...sessionConfig, timerDuration: duration, settings: { ...sessionConfig.settings, ai_configuration: { ...(sessionConfig.settings?.ai_configuration || {}), timerDuration: duration } } }); }; const timerEnabled = sessionConfig.timerEnabled || sessionConfig.settings?.ai_configuration?.timerEnabled || false; const timerDuration = sessionConfig.timerDuration || sessionConfig.settings?.ai_configuration?.timerDuration || 5; return ( <div className='p-4'> <TimerSettings timerEnabled={timerEnabled} timerDuration={timerDuration} onTimerEnabledChange={handleTimerEnabledChange} onTimerDurationChange={handleTimerDurationChange} /> </div> ); }; export default AIInteractionConfig;
