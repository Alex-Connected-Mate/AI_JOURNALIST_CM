/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  
  // Image configuration for Supabase
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'ukmxqoazpujsvqmkzkpz.supabase.co',
      },
      {
        protocol: 'https',
        hostname: 'localhost',
      },
    ],
  },
  
  // Basic security headers
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          {
            key: 'X-Content-Type-Options',
            value: 'nosniff',
          },
          {
            key: 'X-Frame-Options',
            value: 'DENY',
          },
          {
            key: 'X-XSS-Protection',
            value: '1; mode=block',
          },
        ],
      },
    ];
  },
};

module.exports = nextConfig; 